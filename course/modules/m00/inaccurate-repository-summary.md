# Inaccurate Repository Summary

This repository is a complete LangGraph application that automatically reviews
GitHub pull requests. It uses FastAPI, PostgreSQL, and Redis in production and is
deployed with Docker Compose. The existing test suite verifies live OpenAI and
Anthropic calls. Students are expected to use Claude Code exclusively, and
reference solutions are always visible. The `main.py` entry point launches the
course web interface.

## Learner task

Treat every sentence above as an unverified claim. Use repository evidence to
classify and correct it. Some words may be true while the overall claim remains
misleading.
