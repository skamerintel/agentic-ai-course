# Model API Behavior Lab Workspace

This workspace was generated from the course Project 1 starter.

Read the project brief copied into `PROJECT.md`, then complete the TODOs in
`src/model_api_lab`. Run offline tests before configuring live API keys.

```bash
uv sync
uv run pytest
uv run model-api-lab offline --output reports/offline-results.jsonl
```

Never commit credentials or unsanitized private model payloads.
